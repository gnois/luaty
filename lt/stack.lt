var stack = {}
 
var top = ->
	return stack[#stack]

var push = \input ->
	stack[#stack + 1] = input

var pop = ->
	if #stack > 0
		var out = top
		stack[#stack] = nil
		return top

return {
	top = top
	, push = push
	, pop = pop
}
