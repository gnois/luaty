
return ->
	var stack, s = {}, 0

	return {
		top = ->
			return stack[s]

		, push = \val ->
			s = s + 1
			stack[s] = val

		, pop = ->
			if s < 1
				return nil, "stack empty"
			var val = stack[s]
			stack[s] = nil  -- allow garbage collect
			s = s - 1
			return val
	}
