var foo = \x, y ->
    if x < y and x == y 
        return x
    else
        return y
    


var boo = \x, y->
    if x < y or x == y 
        return x
    else
        return y
    


var x, y = 3, 7
print(foo(x, y), foo(y, x))
print(boo(x, y), boo(y, x))
