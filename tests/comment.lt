    		
	  -- comments lines shd be ignored regardless of tab or space indented

--`` This is `````
a very long comment
``

  -- this is ```short
              			-- `one
	-- dangling comment 
print(0)
   -- space indented dangling comment
  
	-- dangling again
var f = \a, b->  -- comment after code
         --   space indented comment in block
	print (a)
        -- 
    --``` long string comment
	 another line tab indented
      another line space indented ```
  
	print(b)
	-- at end of block
  --                        another one space indented
	
-- empty comment below
--         
print(1)

var   --`` embed ]] ``  foo, bar = 3, 7
print("foo bar"--`` embedded ] --` `  ``, foo, bar)


  
   
   --````
	This is un elaborate Lua-style, long comment.
  space indented
	    		 space mix with tab   	
  
]==] ``

	    --````
		
	   -- at end of file
	   
-- Long comment trick at https://www.lua.org/pil/1.3.html


--`
print(10)         -- no action (commented)
---`              -- use more than 2 hyphens at the end of comment

-- Now, if we add a single hyphen to the first line, the code is in again:

---`
print(20)         --> 10
---`
