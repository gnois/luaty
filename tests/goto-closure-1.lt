var ls = { }

for z=1,10 
    for y=1,10 
        for x=1,10 
            if x^2 + y^2 == z^2 
                ls[#ls+1] = fn() 
                    return x, y, z 
                goto zcontinue

    ::zcontinue::

for i = 1, #ls
    print('found a Pythagorean triple', ls[i]())

