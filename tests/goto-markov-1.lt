-- Use a very weak pseudo-number generator just for testing purpose.
var my_random = \s->
    s.x = (16807 * s.x) % 2147483647
    return s.x % 50


var markov = ->
    var s = { x = 13 }
    ::a::
    print('A')
    if my_random(s) < 10 
        goto c 
    ::b::
    print('B')
    if my_random(s) < 20 
        goto d 
    ::c::
    print('C')
    if my_random(s) < 5
        goto a 
    ::d::
    print('D')
    if my_random(s) < 48  
        goto b
    else
        return 


markov()
