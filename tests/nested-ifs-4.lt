var foo = \x, y->
    if x < y 
        return x + y 
    else if x + y < x - y
        return x - y
    else if x * y < x / y
        return x * y
    else
        return x / y



print(foo(3, 4), foo(4, 3))
